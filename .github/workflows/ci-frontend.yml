name: Frontend CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'multi-vendor-frontend/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'multi-vendor-frontend/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: multi-vendor-frontend

    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'multi-vendor-frontend/package-lock.json'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Lint
      run: npm run lint
      
    - name: Build
      run: npm run build
